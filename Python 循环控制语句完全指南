Python 循环控制语句完全指南
📋 目录
基本循环语句

循环控制语句

高级迭代技巧

itertools模块

推导式与生成器

性能优化

实用模式

1. 基本循环语句
1.1 for 循环
python
# 基本形式
for item in sequence:
    # 循环体
    pass

# 示例
fruits = ["apple", "banana", "cherry"]
for fruit in fruits:
    print(fruit)
1.2 while 循环
python
# 基本形式
while condition:
    # 循环体
    pass

# 示例
count = 0
while count < 5:
    print(count)
    count += 1
2. 循环控制语句
2.1 break - 立即退出循环
python
for i in range(10):
    if i == 5:
        break  # 当i等于5时退出循环
    print(i)  # 输出: 0 1 2 3 4
2.2 continue - 跳过本次迭代
python
for i in range(5):
    if i == 2:
        continue  # 跳过i=2的情况
    print(i)  # 输出: 0 1 3 4
2.3 pass - 空操作占位符
python
for i in range(3):
    if i == 1:
        pass  # 什么也不做，只是占位
    else:
        print(i)  # 输出: 0 2
2.4 else - 循环正常结束执行
python
# for循环中的else
for i in range(3):
    print(i)
else:
    print("循环正常结束")  # 会执行

# while循环中的else
i = 0
while i < 3:
    print(i)
    i += 1
else:
    print("循环正常结束")  # 会执行
⚠️ 重要规则：

else 只在循环没有被 break 中断时执行

continue 和 pass 不影响 else 块的执行

3. 高级迭代技巧
3.1 enumerate() - 获取索引和值
python
fruits = ["apple", "banana", "cherry"]
for index, fruit in enumerate(fruits):
    print(f"索引 {index}: {fruit}")

# 指定起始索引
for index, fruit in enumerate(fruits, start=1):
    print(f"第{index}个: {fruit}")
3.2 zip() - 并行迭代多个序列
python
names = ["Alice", "Bob", "Charlie"]
ages = [25, 30, 35]

for name, age in zip(names, ages):
    print(f"{name}: {age}岁")

# 处理不等长序列（以最短的为准）
list1 = [1, 2, 3]
list2 = ['a', 'b']
for a, b in zip(list1, list2):
    print(a, b)  # 输出: 1 a, 2 b
3.3 reversed() - 反向迭代
python
# 反向遍历列表
for item in reversed([1, 2, 3]):
    print(item)  # 3, 2, 1

# 反向遍历range
for i in reversed(range(5)):
    print(i)  # 4, 3, 2, 1, 0
3.4 range() 进阶用法
python
# 倒序
for i in range(10, 0, -1):  # 10到1
    print(i)

# 指定步长
for i in range(0, 10, 2):  # 0, 2, 4, 6, 8
    print(i)
4. itertools模块
python
import itertools

# 无限循环
count = 0
for i in itertools.count(10, 2):  # 从10开始，每次+2
    print(i)
    count += 1
    if count >= 3: break  # 10, 12, 14

# 循环迭代
colors = itertools.cycle(["红", "绿", "蓝"])
for _ in range(5):
    print(next(colors))  # 红, 绿, 蓝, 红, 绿

# 排列组合
for perm in itertools.permutations("ABC", 2):
    print(perm)  # AB, AC, BA, BC, CA, CB

for comb in itertools.combinations("ABC", 2):
    print(comb)  # AB, AC, BC
5. 推导式与生成器
5.1 列表推导式
python
# 基本形式
squares = [x**2 for x in range(10)]

# 带条件
even_squares = [x**2 for x in range(10) if x % 2 == 0]

# 复杂条件
numbers = [x if x % 2 == 0 else x*10 for x in range(10)]
5.2 生成器表达式（节省内存）
python
# 列表推导式（立即计算）
big_list = [x**2 for x in range(1000000)]  # 占用大量内存

# 生成器表达式（惰性计算）
big_gen = (x**2 for x in range(1000000))  # 几乎不占内存
for value in big_gen:
    if value > 100:
        break
5.3 字典和集合推导式
python
# 字典推导式
square_dict = {x: x**2 for x in range(5)}

# 集合推导式
unique_squares = {x**2 for x in range(-5, 6)}
6. 性能优化
6.1 避免重复计算
python
# ❌ 不好的写法
my_list = [1, 2, 3, 4, 5]
for i in range(len(my_list)):
    print(my_list[i])

# ✅ 好的写法
for item in my_list:
    print(item)

# ✅ 需要索引时
for i, item in enumerate(my_list):
    print(f"索引{i}: {item}")
6.2 使用局部变量
python
# 不好的写法
def process_list(items):
    for i in range(len(items)):
        print(items[i])

# 好的写法
def process_list(items):
    length = len(items)  # 缓存长度
    for i in range(length):
        print(items[i])
7. 实用模式
7.1 跳出多重循环
python
# 方法1：使用标志变量
found = False
for i in range(5):
    for j in range(5):
        if i * j == 6:
            found = True
            break
    if found:
        break

# 方法2：使用函数返回
def find_pair():
    for i in range(5):
        for j in range(5):
            if i * j == 6:
                return i, j
    return None
7.2 模拟 do-while 循环
python
# Python没有do-while，但可以模拟
while True:
    # 至少执行一次
    response = input("继续吗? (y/n): ")
    if response.lower() != 'y':
        break
7.3 使用 _ 忽略变量
python
# 忽略不需要的变量
for _ in range(10):
    print("Hello")

# 解包时忽略某些值
points = [(1, 2, 3), (4, 5, 6)]
for x, _, z in points:
    print(f"x={x}, z={z}")
7.4 循环中的异常处理
python
for i in range(5):
    try:
        result = 10 / i
    except ZeroDivisionError:
        print(f"除以零错误: i={i}")
        continue
    else:
        print(f"结果: {result}")
    finally:
        print(f"迭代完成")
🎯 核心要点总结
break vs continue vs pass：

break: 完全退出循环

continue: 跳过本次迭代，继续下一次

pass: 空操作，什么也不做

循环else的特殊性：

只在循环正常结束（未遇到break）时执行

与if-else中的else含义完全不同

性能优先：

尽量直接迭代，而不是使用索引

使用生成器处理大数据

缓存重复计算的变量

Pythonic写法：

使用enumerate()获取索引

使用zip()并行迭代

善用推导式简化代码

📚 最佳实践
清晰优于简洁：复杂的推导式可读性差，考虑使用普通循环

适当使用工具：itertools提供了很多高效迭代工具

注意内存使用：大数据集时使用生成器而非列表

利用循环else：简化"查找-未找到"逻辑

